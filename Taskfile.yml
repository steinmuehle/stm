version: '3'

env:
  IMG: jottr/middleman-stm

tasks:
  build:
    cmds:
      - docker build -t $IMG .
  clean:
    cmds:
      - docker rmi $IMG
  run:
    cmds:
      - docker run -it --rm -p 4567:4567 -v "${PWD}":/app --platform linux/amd64 $IMG
